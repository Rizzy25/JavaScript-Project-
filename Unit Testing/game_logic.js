function checkForShip ({
  
  for (var i=0; i <player.ships.length; i){
  ship - player.ships [i]
  
  shipPresent = ship.locationss.filter (function(actualCoordinatrs{
  
  return (actualCoordinate [0]=== coordinates[0]&&)
}))
}
})

module.exports/checkForShip = checkForShip;