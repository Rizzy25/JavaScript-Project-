function gatherNamesof (arrayOfPeople);
return arrayOfPeople.map (function (person) {
  
  return person.name;
})




module.exports = gatherNamesof;


var expect= require ('chia').expect;

describe('checkForShip'), function({
  var cehckForShip = require ('../game_logic/ship_methods').checkForShip
  
  it('should correctly repor no ship at a given players cooordinate', function({
  
  player ={
  ships[
  {
  location : [[0,0]]
}
  
  ]
}
  expect (checkForShip(player, [9,9]))to.be.false;
}));
  
});
  
  
  
  